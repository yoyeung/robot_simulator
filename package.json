{
  "name": "code_test",
  "version": "1.0.0",
  "description": "code test",
  "scripts": {
    "test": "jest",
    "test:sampleData": "jest  --testNamePattern='Sample Data'",
    "test:watch": "jest --watchAll",
    "start:dev": "babel-node src/index.js",
    "start": "npm run build && node dist/index.js",
    "build": "npm run clean && babel src -d dist",
    "clean": "rm -rf dist",
    "coverage": "jest --coverage"
  },
  "keywords": [],
  "author": "Moses Yeung",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "jest": "^23.1.0"
  }
}
